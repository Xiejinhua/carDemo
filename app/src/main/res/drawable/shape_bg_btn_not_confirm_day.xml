<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充的颜色 -->
    <solid android:color="@color/customColorNotConfirmDay"/>
    <corners android:radius="@dimen/sv_dimen_40" />

    <!-- android:radius 弧形的半径宽度 -->
    <stroke android:color="@color/customColorNotConfirmDay"
        android:width="@dimen/sv_dimen_0" />
</shape>