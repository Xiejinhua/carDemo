<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 指定圆角半径 -->
    <corners
        android:radius="@dimen/sv_dimen_30"/>

    <gradient
        android:angle="270"
        android:endColor="@color/customColorActivationBgEndColorNight"
        android:startColor="@color/secondaryContainerNight"
        android:type="linear" />

    <!-- android:radius 弧形的半径宽度 -->
    <stroke android:color="@color/customColorDialogLinkNight"
        android:width="@dimen/sv_dimen_1" />
</shape>